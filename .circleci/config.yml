version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: yarn install
          working_directory: ./CI-project
      - run:
          name: Run Tests
          command: yarn run test:devnet:ci
          working_directory: ./CI-project

workflows:
  version: 2
  build-deploy:
    jobs:
      - build